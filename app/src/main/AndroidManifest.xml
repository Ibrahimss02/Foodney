<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_foodney"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_foodney_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodney"
        tools:targetApi="31">
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyABql2UNuCa3P9HWfwbWnsYTC4PslPQoho" />

        <activity
            android:name=".MainActivity"
            android:launchMode="standard"
            android:exported="false">
            <nav-graph android:value="@navigation/main_navigation" />
        </activity>

        <receiver android:name=".utils.GeofenceBroadcastReceiver" />
    </application>

</manifest>